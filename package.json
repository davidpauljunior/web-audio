{
  "name": "web-audio-api",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build:js": "mkdir -p dist/js && browserify ./src/js/main.js -o ./dist/js/main.js -t babelify --presets es2015",
    "watch:js": "onchange './src/js/**/*.js' -- npm run build:js",
    "dev": "npm run build:js && npm run watch:js",
    "test": "jest"
  },
  "author": "David Paul",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.14.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "jest": "^20.0.4",
    "onchange": "^3.2.1"
  }
}
